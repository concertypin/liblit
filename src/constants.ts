/** Maximum upstream response bytes (~96 MB) */
export const MAX_RESPONSE_BYTES = 96 * 1024 * 1024;

/** Default base URL for the NovelAI Image API. */
export const DEFAULT_BASE_URL = "https://image.novelai.net";
